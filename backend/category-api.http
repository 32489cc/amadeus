### 分类 API 手动测试
### 用法：装 VS Code「REST Client」扩展（或 IntelliJ 自带 HTTP Client），
###       点每个请求上方的 "Send Request" 即可。
@baseUrl = http://localhost:8080/api

### 1. 查全部分类（树形，带 children）
GET {{baseUrl}}/categories

### 2. 更新分类 —— 部分更新：只传想改的字段，其余不动
###    id 在路径上；name/slug/productCount/tone/sortOrder 选填
PUT {{baseUrl}}/categories/cat01
Content-Type: application/json

{
  "name": "フィギュア（测试改名）",
  "tone": "b"
}

### 3. 改回原值（测完还原）
PUT {{baseUrl}}/categories/cat01
Content-Type: application/json

{
  "name": "フィギュア",
  "tone": "a"
}

### 4. 边界：id 不存在 → 期望 404 + "分类不存在"
PUT {{baseUrl}}/categories/not-exist-id
Content-Type: application/json

{
  "name": "x"
}

### 5. 再查一次确认结果
GET {{baseUrl}}/categories
